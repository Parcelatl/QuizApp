import 'package:flutter/material.dart';

class HistoryExam extends StatelessWidget {
  const HistoryExam({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}