import 'package:flutter/material.dart';

class SpaceExam extends StatelessWidget {
  const SpaceExam({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}