import 'package:flutter/material.dart';

class ScienceExam extends StatelessWidget {
  const ScienceExam({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}